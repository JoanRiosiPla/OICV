#include <iostream>
#include <vector>
#include <string.h>
#include <algorithm>

#include <random>
#include <time.h>

using namespace std;

vector<int> n, k;
n.push_back(13);
sort(n.begin(), n.end(), &sorter);
bool sorter(int n, int p) {
    return n < p;
}

cin >> c;
cout << "Hi" << 12;

string str = "Hi";
str.size()

srand(time(NULL)); // Start rand
rand() % 10000
